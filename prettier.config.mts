import { type Config } from 'prettier';

const config: Config = {
  trailingComma: 'none',
  semi: true,
  singleQuote: true,
  printWidth: 100,
  tabWidth: 2,
  bracketSpacing: true,
  arrowParens: 'always'
};

export default config;
